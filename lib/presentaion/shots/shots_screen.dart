import 'package:flutter/material.dart';

class ShotsScreen extends StatelessWidget {
  const ShotsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Shots Screen"),
      ),
    );
  }
}
